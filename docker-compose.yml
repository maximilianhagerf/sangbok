services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - db-data:/app/server/data
    environment:
      - NODE_ENV=production
      - PORT=3001

volumes:
  db-data:
